# Copy to .env (loaded by docker-compose) for local secrets / overrides.
# All production values are injected by the deploy pipeline.

# --- Django ---
SECRET_KEY=change-me-in-prod
DEBUG=False
ALLOWED_HOSTS=cv.chlous.top,localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=https://cv.chlous.top
CSRF_TRUSTED_ORIGINS=https://cv.chlous.top
LOG_LEVEL=INFO

# --- Database ---
DATABASE_URL=postgres://mycv:mycv@db:5432/mycv

# --- Sentry (Phase 4) ---
# SENTRY_DSN=
# SENTRY_ENVIRONMENT=production

# --- Frontend ---
VITE_API_BASE=/api
